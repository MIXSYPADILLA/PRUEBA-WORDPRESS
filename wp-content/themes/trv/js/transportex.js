jQuery(document).ready(function() {

	/* Slider section */
	wp.customize.section( 'sidebar-widgets-sidebar-slider' ).panel( 'homepage_setting' );
	wp.customize.section( 'sidebar-widgets-sidebar-slider' ).priority( '1' );
	
});